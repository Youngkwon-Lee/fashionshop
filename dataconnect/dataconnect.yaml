specVersion: "v1beta"
serviceId: "o-won-clone"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "o-won-clone-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
